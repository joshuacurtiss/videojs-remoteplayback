# Example Implementation of videojs-remoteplayback

This is an orphan branch that is an example implementation consuming the built plugin.

To try it, just run `npm install` and then `npm run dev`. A dev server will be started
that is using a built instance of the project in its dependencies.
